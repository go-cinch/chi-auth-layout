# Code generated by docsgen; DO NOT EDIT.

openapi: 3.0.3
info:
  title: "{{ .Computed.service_name_kebab }} API"
  version: 1.0.0
servers:
  - url: "https://example.com/api/{{ .Computed.service_name_kebab }}"
    description: "prod"
  - url: "https://dev.example.com/api/{{ .Computed.service_name_kebab }}"
    description: "dev"
  - url: "http://127.0.0.1:{{ .Scaffold.http_port }}"
    description: "local"
{{ if .Computed.enable_health_check_final -}}
paths:
  /healthz:
    get:
      summary: "GET /healthz"
      operationId: getHealthz
      tags: ["Server"]
      responses:
        "200":
          description: "OK"
        "405":
          description: "Method Not Allowed"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: "Service Unavailable"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  schemas:
    ErrorResponse:
      type: object
      required: [msg]
      properties:
        msg:
          type: string
{{ else -}}
paths: {}
{{ end -}}
